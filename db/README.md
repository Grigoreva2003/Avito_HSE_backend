# Database Migrations

Эта папка содержит SQL миграции для базы данных проекта.

## Структура

- `V001__initial_schema.sql` - Создание таблиц `sellers` и `ads`
- `V002__seed_data.sql` - Вставка тестовых данных

## Схема БД

### Таблица `sellers` (Продавцы/Пользователи)

| Колонка      | Тип                    | Описание                          |
|--------------|------------------------|-----------------------------------|
| id           | SERIAL PRIMARY KEY     | Уникальный идентификатор          |
| name         | VARCHAR(255) NOT NULL  | Имя продавца                      |
| is_verified  | BOOLEAN DEFAULT FALSE  | Статус верификации (подтвержден?) |
| created_at   | TIMESTAMP WITH TZ      | Дата создания                     |
| updated_at   | TIMESTAMP WITH TZ      | Дата обновления                   |

### Таблица `ads` (Объявления)

| Колонка     | Тип                     | Описание                              |
|-------------|-------------------------|---------------------------------------|
| id          | SERIAL PRIMARY KEY      | Уникальный идентификатор              |
| seller_id   | INTEGER NOT NULL (FK)   | Ссылка на продавца                    |
| name        | VARCHAR(500) NOT NULL   | Название товара                       |
| description | TEXT NOT NULL           | Описание товара                       |
| category    | INTEGER NOT NULL        | Категория товара                      |
| images_qty  | INTEGER DEFAULT 0       | Количество изображений (>= 0)         |
| created_at  | TIMESTAMP WITH TZ       | Дата создания                         |
| updated_at  | TIMESTAMP WITH TZ       | Дата обновления                       |

### Связи

- `ads.seller_id` → `sellers.id` (ON DELETE CASCADE)

### Индексы

- `idx_ads_seller_id` - индекс на `ads.seller_id` для быстрого поиска объявлений продавца
- `idx_sellers_is_verified` - индекс на `sellers.is_verified` для фильтрации по статусу

## Применение миграций

### Вариант 1: Использование yandex-pgmigrate (рекомендуется)

```bash
# Создайте БД
createdb avito_moderation

# Примените миграции (из папки db/)
cd db
pgmigrate -t latest migrate
cd ..
```

💡 **Примечание:** Параметры подключения указаны в `db/migrations.yml`. Измените их при необходимости.

### Вариант 2: Ручное применение через psql

```bash
# Создайте БД
createdb avito_moderation

# Примените миграции по очереди
psql avito_moderation -f db/V001__initial_schema.sql
psql avito_moderation -f db/V002__seed_data.sql
```

## Проверка

```bash
# Проверьте структуру таблиц
psql avito_moderation -c "\d sellers"
psql avito_moderation -c "\d ads"

# Посмотрите данные
psql avito_moderation -c "SELECT * FROM sellers;"
psql avito_moderation -c "SELECT * FROM ads LIMIT 5;"
```

## Тестовые данные

После применения миграций в БД будут:

- **5 продавцов** (sellers):
  - 3 верифицированных (is_verified=true)
  - 2 неверифицированных (is_verified=false)

- **7 объявлений** (ads):
  - С разным количеством изображений (0-10)
  - В разных категориях (1, 2, 5, 10, 15, 20)
  - От разных продавцов

## Откат миграций

Для отката можно удалить и пересоздать БД:

```bash
dropdb avito_moderation
createdb avito_moderation
pgmigrate -t latest migrate
```
